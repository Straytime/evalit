# CHG-20260920-004：同步项目协作规则与 README

## 技术设计（先于测试与入口文档修改）

- 来源：用户要求依据最新完整技术设计检查并按需更新 AGENTS.md、README.md。
- 类型：文档；基线 commit 为 2759888，开始时工作区干净。
- 依据：techv_0.2 的架构、持久化、沙盒、Cloud/Trace、恢复、安全、交付及里程碑章节；TDD 工作流；ENG-TDD / TC-DOC-01。
- 发现：现有入口未明确最新进程/编译隔离和 Trace Metadata 约束，README 未标明已审查基线、开发环境与成品依赖的区别、真实集成待验证边界；开发者需跳读长文才能识别这些前提。
- 范围：AGENTS.md、README.md、tests/docs/technical-design.test.mjs、本记录；不改产品契约、PRD、原型、主技术设计、依赖、CI 或远端设置。
- 原则：入口提炼已有规则并链接权威章节，不复制所有 DTO、数值限额和验收规格；禁止将已选方案写成已实现能力。

### 验收契约

1. AGENTS 明确阅读顺序、CHG 与有效 RED/GREEN、变更影响下的验收规格/追踪同步、门禁防绕过和平台重放；提供按工作类型定位设计的入口。
2. AGENTS 提示 Main broker/Renderer/Core 边界、QuickJS 与纯内存 CompilerHost、独立阶段 CAS/不可变快照/产出持久化、Cloud 只读及 Trace Metadata 语义、无自动重试/续跑、安全凭证与测试隔离，不改写已有设计。
3. README 标明与主文档一致的 PRD/技术设计版本，区分规划功能、已有文档检查与未实施的产品/M0/CI；说明选定架构和当前可运行命令。
4. README 提供 M0/后续里程碑、外部验证与安全边界链接；区分开发用 Node/pnpm 和成品不依赖用户安装运行时；不提供不存在的启动/构建脚本。
5. 新增入口检查先在旧文件上得到有效 RED，再更新两份入口文档；现有 16 项检查必须保持通过，所有本地链接/锚点有效。

### 风险与回滚

入口重复规则可能漂移，因此以章节链接为主；摘要变化必须对照主设计，不成为第二份技术规范。仅文档与检查，无数据迁移或外部副作用。若需撤销，仅回退本 CHG 对应改动，保留已有技术设计；本次不自动 commit/push。

## 先行测试与交付记录

命令：node --test tests/docs/technical-design.test.mjs。新增检查仅验证文档入口与既有设计的显式一致性，不验证尚未实现的产品行为。

- RED：Node v24.19.0，退出码 1，原 16 项通过、新增 4 项失败；分别提示 AGENTS 缺测试策略入口、缺 Main broker 约束，README 缺最新设计版本、缺里程碑入口。无依赖/编译/网络故障充当 RED。
- 实现：AGENTS 分为阅读顺序、TDD、架构边界、任务导航、验证交付；README 补充基线与未实施状态、规划能力、架构/运行时说明、M0 入口、数据与外部验证边界。仅提炼现有契约，不修改技术选型。
- GREEN：同一命令退出码 0，20/20 通过、0 fail/skip；git diff --check 通过。既有需求/场景追踪检查保持通过，无新增产品场景或产品行为，矩阵不变。
- 复核：读取最新 techv_0.2 并对照本次 diff；所有摘要均有对应设计来源。明确 Langfuse 只接收读取所需查询，不上传测试集或评估结果；不把本地存储描述成全库加密。
- 证据：设计记录、先行断言、RED 输出和修改后的 GREEN 按序保留在本任务工具记录中；本次未创建 D/T/I 提交或受信 CI bundle，不虚构阶段 SHA。
- 交付：只更新两份入口及配套文档检查/CHG，未实现产品、未修改远端、未 commit/push。工程门禁及真实平台/Cloud 验证仍以 M0 和后续实施结果为准。
