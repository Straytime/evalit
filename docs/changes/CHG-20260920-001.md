# CHG-20260920-001：技术设计与 TDD 规范基线

## 技术设计（先于测试与实现）

- 日期：2026-09-20。
- 需求：为 PRD docv_0.3 和已提交设计原型编写完整技术设计；所有后续变更严格按“技术设计 → 测试 → 实现”进行。
- 输入基线：Git `a58d008`；用户补充 Langfuse 使用 SaaS。
- 变更类型：文档与工程治理基线；不实现产品功能，不修改 PRD 或设计原型。
- 产物：架构及模块设计、数据与运行契约、集成协议、测试规格、需求追踪矩阵、TDD 规则、变更模板及项目入口。
- 方案：以 Electron / React / TypeScript、SQLite、隔离 JS 引擎为设计基线；Cloud 集成的语义差异单独记录。
- 约束：区分“设计中的测试”与“已执行的测试”；不能声称尚未建立的 CI 或远端保护已经生效。

## 验收测试设计

1. 所有承诺交付的文档存在，文档间相对链接及显式锚点均可解析。
2. 从真实 PRD 提取 func 编号，追踪矩阵恰好覆盖全部 20 项，不能缺项或使用重复编号掩盖缺项。
3. 每项需求引用的技术设计锚点、测试场景均存在；测试场景有前置条件、操作、预期和层级。
4. 额外工程要求（TDD、凭证、迁移、崩溃恢复、UI、集成兼容）也有场景，且不得冒充已实现测试。
5. 根目录协作规则和 README 引用有效规范；规范明确严格次序、RED/GREEN 证据、文档变更规则、CI 防绕过及当前实施状态。

校验命令：`node --test tests/docs/technical-design.test.mjs`。使用 Node 内置测试运行器，不安装依赖。校验脚本只读仓库，不调用外部服务。

## 测试与实现证据

- 工具链：本机 Node v24.19.0；无第三方依赖安装。
- RED：已实际执行同一命令，退出码 1，5 项失败、0 项通过。首项断言为“缺少产物：docs/technical_docs/evalit_v001_technical_design.md”；其余缺少矩阵/场景/入口。这是文档产物缺失的预期失败，不是产品测试运行结果。
- GREEN：已实际执行同一命令，退出码 0，5 项通过、0 项失败/跳过。校验覆盖 PRD 全部 20 个 func、6 项工程要求与 80 组验收场景的追踪和相对链接。
- 实现：新增技术设计、测试策略、TDD 工作流、决策记录、追踪矩阵、变更模板和根目录协作规则，更新 README 入口。
- 自审：补充了 Cloud SaaS 接口差异、metadata 查询 filter 优先级、完整分页/展开、SQLite 唯一键与 CAS、运行排队时的快照时点、无自动重试的恢复契约。
- 补充检查：git diff --check 通过；未改产品 PRD 或原型，未运行未实现的产品测试，未 commit/push。
- 执行证据：本次任务工具日志保留先创建 CHG、创建测试、RED、文档实现、GREEN 的实际顺序；本次尚无 CI artifact 或 D/T/I 分阶段 commit，不伪造哈希或远端门禁状态。
- 后续修正：用户已重申关联 Trace 自身 Metadata，原“根/任意 Observation”问题撤回，见 [CHG-20260920-002](CHG-20260920-002.md)；真实账号与签名验证仍留在对应实施切片。
- 限制：文档结构与追踪校验不能证明产品已实现，也不能证明未来开发者实际遵守 TDD；后者还需执行规范规定的可信 CI 与远端分支保护。
